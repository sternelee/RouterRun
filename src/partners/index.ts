/**
 * X402: Partner API Integration — barrel export
 *
 * X402: This entire module is for x402 payment mode with BlockRun API proxy.
 * X402: For API Key mode, this functionality is disabled.
 *
 * Partner APIs provide additional services (Twitter/X lookup, etc.) via the
 * x402 BlockRun API proxy. In API Key mode, direct provider access is used.
 */

// X402: export { PARTNER_SERVICES, getPartnerService } from "./registry.js";
// X402: export type { PartnerServiceDefinition, PartnerServiceParam } from "./registry.js";
// X402: export { buildPartnerTools } from "./tools.js";
// X402: export type { PartnerToolDefinition } from "./tools.js";
